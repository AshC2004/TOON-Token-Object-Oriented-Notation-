# TOON Converter Requirements

# Core dependencies (none - uses only Python stdlib!)
# Python 3.10+ required for ast.unparse()

# Development dependencies (optional)
pytest>=7.4.0
black>=23.0.0
mypy>=1.0.0
